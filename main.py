# The 3D presentation app is a Vite + React project now — see README.md for the
# full npm flow (npm install → npm run dev).
#
# This file was a stub for a Python generation script that is no longer part of
# the project. The API key now lives in `.env` as VITE_DEEPSEEK_API_KEY
# (see .env.example).
